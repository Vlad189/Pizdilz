
#scripted_diplomatic_actions = {
#
#	action_key = { # will be used in most localizations
#	
#		# root is the initiator of action and this is the target country
#		allowed = { #if action is valid
#			always = yes
#		}
#		visible = { #if action is visible on gui
#			always = yes
#		}
#		selectable = { #if action is selectable on gui
#			always = yes
#		}
#
#		requires_acceptance = yes # if true the action will require acceptance of target country
#		cost = 10 # pp cost, can be zero
#		command_power = 10 # cp cost, can be zero
#
#		cost_string = loc_key # if you have a custom cost, use this loc to display it
#		show_acceptance_on_action_button = yes # set to no if acceptance shouldn't be displayed on the action button
#
#		icon = 1 # icon frame to use in notification
#
#       #for effects/triggers/variables, root is the sender and this is receiver
#
#		# runs when the action is sent
#		on_sent_effect = {
#		}
#
#		# runs when the action is properly sent and accepted
#		complete_effect = {
#		}
#
#		# runs when the action is properly sent and rejected
#		reject_effect = {
#		}
#
#		# SEND POPUP CUSTOMIZATION
#		# use diplomatic_action as context in scripted gui
#		# send_scripted_gui = send_scripted_gui_name # if specified, the diplomatic action will use this scripted gui before sending the request. the player (or ai) can make changes on that gui and store stuff as variable to customize action
#		reset_send_effect = { # if action has a send scripted gui, this effect will be used for clearing the state of gui (will run when player opens the scripted gui by sending the action or when ai proposes action
#			# clear action
#			if = {
#				limit = { 
#					root = {
#						is_ai = yes
#					}
#				}
#				# initialize for ai
#			}
#		}
#		can_be_sent = { # send button will be disabled if no
#			always = yes
#		}
#		send_description = loc_key_send # if there is no scripted gui, this desc will be used in send yes/no pop up
#
#
#		# RECEIVE POPUP CUSTOMIZATION
#		# use diplomatic_action as context in scripted gui
#		# receive_scripted_gui = receive_scripted_gui_name #if specified the receiver of the action will get this scripted gui instead of yes-no popup when they receive the propose
#		reset_receive_effect = {
#			# clear action
#			if = {
#				limit = { 
#					is_ai = yes
#				}
#				# change stuff for ai
#			}
#		}
#		can_be_accepted = { # accept button will be disabled if no
#			always = yes
#		}
#		receive_description = loc_key_receive # if there is no scripted gui, this desc will be used in send yes/no pop up
#
#		
#		# PLAYER FEEDBACK ON ACCEPT/REJECT
#		accept_title = loc_key_accept #can be overridden to customize accepted/rejected pop ups for the sender
#		accept_description = loc_key_accept_desc
#		reject_title = loc_key_reject
#		reject_description = loc_key_reject_desc
#	
#		
#		# AI
#		# a list of ai_will_do entries that will be used to decide if AI should accept or reject a request
#		ai_acceptance = {
#			condition = { # name is used for loc
#				base = 100
#			}
#		}
#
#		# ai_will_do to decide if AI should send this action or not
#		ai_desire = {
#			base = -1
#			modifier = {
#				ROOT = {
#					tag = ENG
#				}
#				tag = GER
#				add = 1000
#			}
#		}
#	}
#}


scripted_diplomatic_actions = {


	TE_WAR_OF_INDEPENDENCE = { # will be used in most localizations 
	
		# root is the initiator of action and this is the target country
		allowed = { #if action is valid
			always = yes
		}
		visible = { #if action is visible on gui
			ROOT = { is_subject_of = PREV }
		}
		selectable = { #if action is selectable on gui
			has_war = no
			NOT = { has_idea = recent_peace_treaty }
			
			OR = {
				ROOT = { has_autonomy_state = autonomy_puppet }
				ROOT = { has_autonomy_state = autonomy_colony }
				ROOT = { has_autonomy_state = autonomy_reichsprotectorate }
				ROOT = { has_autonomy_state = autonomy_reichsprotectorate }
			}
			IF = { 
				limit = { 
					THIS = { tag = JAP }
					ROOT = { tag = MAN }
					has_dlc = "Waking the Tiger" 
				}
				has_completed_focus = MAN_independence_war
			}
				
			has_stability > 0.4
			has_war_support > 0.4 
			ROOT = {
				has_opinion = {
					target = PREV
					value < 0
				}
			}
		}

		requires_acceptance = no # if true the action will require acceptance of target country
		cost = 100 # pp cost, can be zero
		command_power = 0 # cp cost, can be zero

		#cost_string = loc_key # if you have a custom cost, use this loc to display it
		#show_acceptance_on_action_button = yes # set to no if acceptance shouldn't be displayed on the action button

		icon = 1 # icon frame to use in notification

       #for effects/triggers/variables, root is the sender and this is receiver

		# runs when the action is sent
		on_sent_effect = {
			#THIS = {
			#	TE_declare_war_iw = yes 
			#}
			
		}

		# runs when the action is properly sent and accepted
		complete_effect = {
			set_variable = { ROOT.var_TE_war_type@THIS = 6 }
			set_variable = { THIS.var_TE_war_type@FROM = 4 }
			THIS = {
				set_autonomy = {
					target = ROOT
					autonomy_state = autonomy_free
				}
			}
			IF = {
				limit = {
					ROOT = { is_in_faction_with = PREV }
				}
				ROOT = { leave_faction = yes }
			}
						
				
			ROOT = {
				declare_war_on = {
					target = PREV
					type = topple_government
				}
			
			}
			 #ROOT = {
			#	declare_war_on = {
			#		target = PREV
			#		type = annex_everything
			#	}
			#} 
		}

		# runs when the action is properly sent and rejected
		reject_effect = {
		}

		# SEND POPUP CUSTOMIZATION
		# use diplomatic_action as context in scripted gui
		# send_scripted_gui = send_scripted_gui_name # if specified, the diplomatic action will use this scripted gui before sending the request. the player (or ai) can make changes on that gui and store stuff as variable to customize action
		reset_send_effect = { # if action has a send scripted gui, this effect will be used for clearing the state of gui (will run when player opens the scripted gui by sending the action or when ai proposes action
			# clear action
			if = {
				limit = { 
					root = {
						is_ai = yes
					}
				}
				# initialize for ai
			}
		}
		can_be_sent = { # send button will be disabled if no
			always = yes
		}
		send_description = TE_wi_loc_key_send # if there is no scripted gui, this desc will be used in send yes/no pop up


		# RECEIVE POPUP CUSTOMIZATION
		# use diplomatic_action as context in scripted gui
		# receive_scripted_gui = receive_scripted_gui_name #if specified the receiver of the action will get this scripted gui instead of yes-no popup when they receive the propose
		reset_receive_effect = {
			# clear action
			if = {
				limit = { 
					is_ai = yes
				}
				# change stuff for ai
			}
		}
		can_be_accepted = { # accept button will be disabled if no
			always = yes
		}
		receive_description = loc_key_receive # if there is no scripted gui, this desc will be used in send yes/no pop up

		
		# PLAYER FEEDBACK ON ACCEPT/REJECT
		accept_title = TE_wi_loc_key_accept #can be overridden to customize accepted/rejected pop ups for the sender
		accept_description = TE_wi_loc_key_accept_desc  
		reject_title = TE_wi_loc_key_reject
		reject_description = TE_wi_loc_key_reject_desc
	
		
		# AI
		# a list of ai_will_do entries that will be used to decide if AI should accept or reject a request
		ai_acceptance = {
			condition = { # name is used for loc
				base = 100
			}
		}

		# ai_will_do to decide if AI should send this action or not
		ai_desire = {
			base = -1
			modifier = {
				ROOT = {
					strength_ratio = {
						tag = PREV
						ratio > 0.5
					}
				} 
				add = 50
			}
			modifier = {
				ROOT = {
					strength_ratio = {
						tag = PREV
						ratio > 0.75
					}
				} 
				add = 100
			}
			modifier = {
				ROOT = {
					strength_ratio = {
						tag = PREV
						ratio > 1
					}
				} 
				add = 500
			}
			modifier = {
				ROOT = {
					OR = { 
						has_government = fascism
						has_government = neutrality
						has_government = anarchist
					}
				} 
				factor = 1.25
			}
			modifier = {
				THIS = {
					OR = { 
						has_government = fascism
						has_government = neutrality
						has_government = anarchist
					}
				} 
				factor = 0.5
			}
		}
	}

#	TE_offer_mild_terms = { # will be used in most localizations   
#	
#		# root is the initiator of action and this is the target country
#		allowed = { #if action is valid
#			always = yes
#		}
#		visible = { #if action is visible on gui
#			THIS = { has_war_with = ROOT }
#		}
#		selectable = { #if action is selectable on gui
#			ROOT = { NOT = { has_country_flag = TE_sent_request_flag } }
#			THIS = { 
#				surrender_progress > 0.01 
#				OR = {
#					is_in_faction = no 
#					is_faction_leader = yes 
#				}
#				any_owned_state = {
#					is_controlled_by = ROOT 
#				}
#				
#			}
#			IF = {
#				limit = {
#					OR = {
#						check_variable = { THIS.var_TE_war_type@ROOT = 4 }
#						check_variable = { THIS.var_TE_war_type@ROOT = 6 }
#						check_variable = { ROOT.var_TE_war_type@THIS = 4 }
#						check_variable = { ROOT.var_TE_war_type@THIS = 6 }
#					}
#					
#				}
#				custom_trigger_tooltip = {
#					tooltip = TE_NOT_CIVIL_WAR_OR_IND
#					always = no
#				}
#			}
#			ELSE  = {
#				custom_trigger_tooltip = {
#					tooltip = TE_NOT_CIVIL_WAR_OR_IND 
#					always = yes
#				}
#			}
#			
#		}
#
#		requires_acceptance = yes # if true the action will require acceptance of target country
#		cost = 75 # pp cost, can be zero
#		#command_power = 10 # cp cost, can be zero
#
#		#cost_string = loc_key # if you have a custom cost, use this loc to display it
#		show_acceptance_on_action_button = yes  # set to no if acceptance shouldn't be displayed on the action button
#
#		icon = 1 # icon frame to use in notification
#
 #      #for effects/triggers/variables, root is the sender and this is receiver
#
#		# runs when the action is sent
#		on_sent_effect = {
#			ROOT = { set_country_flag = TE_sent_request_flag }
#		}
#
#		# runs when the action is properly sent and accepted
#		complete_effect = {
#			ROOT = { clr_country_flag = TE_sent_request_flag }
#		}
#
#		# runs when the action is properly sent and rejected
#		reject_effect = {
#			ROOT = { clr_country_flag = TE_sent_request_flag }
#		}
##
	#	# SEND POPUP CUSTOMIZATION
#		# use diplomatic_action as context in scripted gui
#		# send_scripted_gui = send_scripted_gui_name # if specified, the diplomatic action will use this scripted gui before sending the request. the player (or ai) can make changes on that gui and store stuff as variable to customize action
#		#reset_send_effect = { # if action has a send scripted gui, this effect will be used for clearing the state of gui (will run when player opens the scripted gui by sending the action or when ai proposes action
#			# clear action
#		#	if = {
#		#		limit = { 
#		#			root = {
#		#				is_ai = yes
#		#			}
#		#		}
#				# initialize for ai
#		#	}
#		#}
#		can_be_sent = { # send button will be disabled if no
#			always = yes
#		}
#		send_description = TE_MILD_TERMS_SEND_DESC # if there is no scripted gui, this desc will be used in send yes/no pop up
 #
#
#		# RECEIVE POPUP CUSTOMIZATION
#		# use diplomatic_action as context in scripted gui
#		# receive_scripted_gui = receive_scripted_gui_name #if specified the receiver of the action will get this scripted gui instead of yes-no popup when they receive the propose
#		reset_receive_effect = {
#			# clear action
#			if = {
#				limit = { 
#					is_ai = yes
#				}
				# change stuff for ai
#			}
#		}
#		can_be_accepted = { # accept button will be disabled if no 
#			always = yes
#		}
#		receive_description = TE_MILD_TERMS_RECIEVE_DESC # if there is no scripted gui, this desc will be used in send yes/no pop up
#
#		
#		# PLAYER FEEDBACK ON ACCEPT/REJECT 
#		accept_title = loc_key_accept #can be overridden to customize accepted/rejected pop ups for the sender
#		accept_description = loc_key_accept_desc
#		reject_title = loc_key_reject
#		reject_description = loc_key_reject_desc 
#	
#		
#		# AI
#		ai_acceptance = {
#			TE_WAR_LENGTH = { # name is used for loc
#				base = -24
#				modifier = {
#					add = var:THIS.var_TE_war_length@ROOT  
#					always = yes
#				}
#			}
#			TE_MAJOR = { # name is used for loc
#				base = 0
#				modifier = {
#					add = -50 
#					is_major = yes
#				}
#			}
#			TE_MORE_WAR = { # name is used for loc
#				base = 0
#				modifier = {
#					add = 25 
#					THIS = {
#						any_country = {
#							NOT = { tag = ROOT }
#							NOT = { is_in_faction_with = ROOT }
#							has_war_with = PREV 
#						}
#					}
#				}
#			}
#			TE_UNRECOGNIZED = {
#				base = 0
#				modifier = {
#					add = -25
#					has_idea = unrecognized_nation
#				}
#			}
#			TE_IDEOLOGY = {
#				base = 0
#				modifier = {
#					add = -25
#					has_government = communism
#				}
#				modifier = {
#					add = -50
#					has_government = fascism
#				}
#				modifier = {
#					add = 5
#					has_government = democratic
#				}
#			}
#			TE_ARMY_STRENGTH = { # name is used for loc
#				base = 0
#				modifier = {
#					add = 5 
#					enemies_strength_ratio > 1
#				}
#				modifier = {
#					add = 10 
#					enemies_strength_ratio > 2
#				}
#				modifier = {
#					add = 10 
#					enemies_strength_ratio > 3
#				}
#				modifier = {
#					add = 10 
#					enemies_strength_ratio > 4
#				}
#			modifier = {
#					add = 10 
#					enemies_strength_ratio > 5
#				}
#				modifier = {
#					add = -10 
#					enemies_strength_ratio < 0.9
#				}
#				modifier = {
#					add = -10 
#					enemies_strength_ratio < 0.8
#				}
#				modifier = {
#					add = -10 
#					enemies_strength_ratio < 0.7
#				}
#				modifier = {
#					add = -25
#					enemies_strength_ratio < 0.7
#				}
#			}
#			
#			TE_WAR_SUPPORT = { # name is used for loc
#				base = 0
#				modifier = {
#					add = -30 
#					has_war_support > 0.5
#				}
#				modifier = {
#					add = -20 
#					has_war_support > 0.25
#				}
#			}
#			TE_STABILITY = { # name is used for loc
#				base = 0
#				modifier = {
#					add = -30 
#					has_stability > 0.5
#				}
#				modifier = {
#					add = -20 
#					has_stability > 0.25
#				}
#			}
			
			
#			TE_CONTROL = {
#				base = 0
#				modifier = {
#					add = var:THIS.num_controlled_states
#					always = yes
#				}
#				modifier = {
#					factor = -1
#					always = yes
#				}
#				
#				modifier = {
#					add = var:THIS.num_owned_states
#					always = yes
#				}
#				modifier = {
#					factor = 2
#					always = yes
#				}
#			}
#			TE_ALL_STATES = { # name is used for loc
#				base = 0
#				modifier = {
#					add = 100  
#					THIS = {
#						all_owned_state = {
#							NOT = { is_controlled_by = PREV }
#							
#						}
#					}
#				}
#			}
#		}

#		# ai_will_do to decide if AI should send this action or not
#		ai_desire = {
#			base = -1
#			modifier = {
#				ROOT = {
#					tag = POL
#				}
#				tag = WUP
#				add = 1000
#			}
#		}
#	}
	
	
	
	
	
}